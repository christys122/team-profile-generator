class Employee {
    constructor(name, id, title, email){// = '') {
    this.name = name;
    
    this.id = id;
    this.title = title;
    this.email = email;
    //this.role = role;
    
    
    }

    getName() {
        return `${this.name} is the ${role}`;
    }
    
    getId() {
        return `${this.id}`;
    }

    getEmail() {
        return `${this.email} is the email`;
    }
    getrole = function() {//returns employee?
        return Employee 
           
        
        //console.log(employee)
    }
};


 module.exports = Employee;