class Employee {
    constructor(name = '') {
    this.name = name;
    this.id = 1;
    this.email = 'nick@example.com';
    this.role = 'Manager';
    
    }

    getName() {
        return `${this.name} is the ${title}`;
    }
    
    getId() {
        return `${this.id}`;
    }

    getEmail() {
        return `${this.email} is the email`;
    }
    getrole = function() {//returns employee?
        return Employee 
           
        
        //console.log(employee)
    }
};


 module.exports = Employee;