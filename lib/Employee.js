class Employee {
    constructor(name = '') {
    this.name = name;
    this.id = 1;
    this.email = 'sally@example.com';
    this.role = 'Engineer';
    
    }

    getName() {
        return `${this.name} is the ${title}`;
    }
    
    getId() {
        return `${this.id}`;
    }

    getEmail() {
        return `${this.email} is the email`;
    }
    getrole = function() {//returns employee?
        return Employee 
           
        
        //console.log(employee)
    }
};


 module.exports = Employee;